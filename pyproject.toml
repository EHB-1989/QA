[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "unittest1"
version = "0.1.0"
description = ""
authors = [
  { name = "Tomy Da Rocha", email = "t@t.fr" },
]
license = "MIT"
dependencies = [
    "selenium",
    "flask",
    "requests",
    "flask_sqlalchemy",
    "locust",
    "gunicorn",
    "waitress",
    "flask_caching",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-mock",
    "black",
    "ruff",
]